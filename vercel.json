{
  "buildCommand": "NODE_ENV=production pnpm --filter @workspace/fitness-tracker run build",
  "outputDirectory": "artifacts/fitness-tracker/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
